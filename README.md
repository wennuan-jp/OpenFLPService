in this file update feature discription according to project code

This project is aiming to build a stackover flow and google drive combination to server for FL Studio(or other DAW, special for FL Studio user cuz this service will resolve FLP File and bring open source champion into FL Studio producer community)

## Technical Feasibility
The project has confirmed that parsing `.flp` files to extract plugin metadata is technically possible using an event-based binary parsing approach.

